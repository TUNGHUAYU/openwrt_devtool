# prebuilt_hello-artifacts

Minimal source tree for `Makefile.prebuilt-artifacts.generic`.

The OpenWrt package does not compile anything. It copies prebuilt files from
this tree into the target root filesystem.
